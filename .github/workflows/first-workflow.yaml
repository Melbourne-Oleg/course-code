name: First Workflow
on: [push]

jobs: 
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo string
              run: echo "Hi Oleg!"
            - name: Mutiply Commands
              run: |
                node -v
                npm -v
